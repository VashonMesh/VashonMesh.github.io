---
const year = new Date().getFullYear();
---

<footer>
  <div class="content">
    <p>&copy; {year} VashonMesh.org, All rights reserved.</p>
    <p><a href="https://vashonsoftware.com" class="com">Site by Vashon Software</a></p>
    <div class="social-links">
      <a href="https://github.com/VashonMesh/VashonMesh.github.io" target="_blank" rel="noopener">
       <img src="/GitHub-Mark-32px.png" alt="Edit on GitHub" style="width:32px; height:32px; vertical-align:middle; margin-right:10px;"/>Edit on GitHub
      </a>
      <a href="https://W7VMI.org" target="_blank" rel="noopener">
       <img src="/w7vmi_new_logo.svg" alt="W7VMI" style="width:32px; height:32px; vertical-align:middle; margin-right:10px; margin-left: 40px;" />Affiliated with W7VMI</a>
    </div>
  </div>
</footer>

<style>
  footer {
    background-color: var(--footer-bg);
    padding: 2rem 1rem;
    margin-top: 4rem;
    border-top: 1px solid var(--border-color);
  }

  .content {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1rem;
  }

  p {
    color: var(--text-secondary);
    margin: 0;
  }

  .com {
    display: inline-block;
    transition: all 0.5s ease;
    color: #667eea;
    text-decoration: none;
    font-size: 1rem;
    font-weight: 400;
  }

  .com:hover {
    transform: scale(1.2) rotate(5deg);
    filter: hue-rotate(180deg) drop-shadow(0 0 8px rgba(102, 126, 234, 0.8));
    animation: colorSwirl 2s infinite;
    font-size: 1.1rem;
    font-weight: 700;
  }

  @keyframes colorSwirl {
    0% { filter: hue-rotate(0deg) drop-shadow(0 0 8px rgba(102, 126, 234, 0.8)); }
    25% { filter: hue-rotate(90deg) drop-shadow(0 0 8px rgba(234, 102, 126, 0.8)); }
    50% { filter: hue-rotate(180deg) drop-shadow(0 0 8px rgba(126, 234, 102, 0.8)); }
    75% { filter: hue-rotate(270deg) drop-shadow(0 0 8px rgba(234, 126, 102, 0.8)); }
    100% { filter: hue-rotate(360deg) drop-shadow(0 0 8px rgba(102, 126, 234, 0.8)); }
  }

  .social-links {
    display: flex;
    align-items: center;
  }

  .social-links a {
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.2s, transform 0.3s ease;
    display: inline-flex;
    align-items: center;
  }

  .social-links a:hover {
    color: #667eea;
    transform: scale(1.1);
  }

  /* Add glow effect to icons in dark mode */
  :global(.dark) .social-links img {
    filter: drop-shadow(0 0 8px rgba(255, 255, 255, 0.4));
    transition: transform 0.3s ease, filter 0.3s ease;
  }

  :global(.dark) .social-links a:hover img {
    filter: drop-shadow(0 0 20px rgba(255, 255, 255, 1));
    transform: scale(1.5);
  }
</style>
